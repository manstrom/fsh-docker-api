version: '3.8'

services:
  api:
    build: .
    container_name: fsh-cars-api
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - DATABASE_PATH=/app/data/cars.db